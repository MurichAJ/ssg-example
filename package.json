{
  "name": "ssg-example",
  "private": true,
  "version": "0.0.1",
  "description": "SSG Example",
  "scripts": {
    "build": "stencil build --verbose",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate",
    "prerender": "stencil build --prerender --verbose",
    "md-to-html": "tsc --p ./scripts && node ./scripts/markdown-to-html"
  },
  "devDependencies": {
    "@stencil/core": "^2.0.0",
    "@types/fs-extra": "^9.0.7",
    "@types/glob": "^7.1.3",
    "@types/marked": "^1.2.2",
    "@types/node": "^14.14.27",
    "fs-extra": "^9.1.0",
    "glob": "^7.1.6",
    "marked": "^2.0.0",
    "rollup-plugin-node-polyfills": "^0.2.1",
    "stencil-router-v2": "^0.6.0",
    "typescript": "^4.1.5"
  },
  "license": "MIT",
  "dependencies": {
    "firebase": "^8.2.6"
  }
}
